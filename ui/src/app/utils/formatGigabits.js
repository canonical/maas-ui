/**
 * Format gigabits with GiB unti
 * 
 * @param {Number} valueInGiB
 * @returns {String} - The formatted value
 */

function formatGigabits(valueInGiB) {
  return `${valueInGiB} GiB`;
}

export default formatGigabits;