import React from "react";

import RepositoryForm from "../RepositoryForm";

export const RepositoryAdd = () => {
  return <RepositoryForm title="Add repository" />;
};

export default RepositoryAdd;
