import SectionHeader from "app/base/components/SectionHeader";

const DiscoveriesListHeader = (): JSX.Element => {
  return <SectionHeader title="Network discovery" />;
};

export default DiscoveriesListHeader;
