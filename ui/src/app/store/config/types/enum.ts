export enum AutoIpmiPrivilegeLevel {
  ADMIN = "ADMIN",
  OPERATOR = "OPERATOR",
  USER = "USER",
}

export enum NetworkDiscovery {
  DISABLED = "disabled",
  ENABLED = "enabled",
}

export enum ConfigMeta {
  MODEL = "config",
}
