export enum PodAction {
  COMPOSE = "compose",
  DELETE = "delete",
  REFRESH = "refresh",
}

export enum PodMeta {
  MODEL = "pod",
  PK = "id",
}

export enum PodType {
  LXD = "lxd",
  VIRSH = "virsh",
}
